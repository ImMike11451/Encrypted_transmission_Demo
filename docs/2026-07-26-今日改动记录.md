# 2026-07-26 今日改动记录

本文记录今天在本次协作中实际保留的项目改动，便于后续继续开发、整理提交记录和准备面试介绍。

## 1. 项目说明与本地演示准备

新增或完善了以下文件：

- `README.md`
- `docs/demo-guide.md`
- `docs/current-architecture.md`
- `scripts/init_db.sql`
- `ServerSeckey/ServerSeckey/server.example.json`
- `ClientSecKey/ClientSecKey/clientA.example.json`
- `ClientSecKey/ClientSecKey/clientB.example.json`

主要内容：

- 用中文说明项目目标、当前可信服务端模型、核心模块和演示流程。
- 提供数据库初始化脚本，包含 `keysn`、`seckeyinfo`、`message_log`、`audit_log` 四张表。
- 提供 Client A、Client B 和服务端的示例配置，支持双客户端演示。
- 说明当前环境要求、启动顺序、数据库验证方式和常见问题。
- 明确当前项目不是端到端加密，服务端在转发过程中可以解密消息。

## 2. 消息安全与权限治理

涉及文件：

- `ClientSecKey/ClientSecKey/ClientOP.cpp`
- `ClientSecKey/ClientSecKey/MessageClient.cpp`
- `ClientSecKey/ClientSecKey/MessageClient.h`
- `ClientSecKey/ClientSecKey/RsaCrypto.cpp`
- `ServerSeckey/ServerSeckey/MessageService.cpp`
- `ServerSeckey/ServerSeckey/MessageService.h`

主要改动：

- 客户端请求 ID、服务端消息 ID、审计 ID 改为“作用域 + 微秒时间戳 + 进程内递增序号”，降低连续演示时的碰撞概率。
- 移除客户端协商后会话密钥输出、服务端消息明文输出等敏感日志。
- 单条消息查询只允许发送方或接收方访问。
- 最近消息列表只允许查询当前请求客户端自身的发送记录。
- 保留必要的解析失败、数据库失败、网络失败等错误日志。

业务影响：

- 协议字段和数据库表结构未改变。
- 消息查询增加了原本缺失的权限限制，属于安全行为收紧。

## 3. 注释与日志收敛

覆盖客户端、服务端的主要业务层和基础设施层，包括：

- `ClientOP`、`MessageClient`、`ServerOP`
- `MessageService`、`MessageRepository`、`AuditService`、`mysqlOP`
- `AesGcmCrypto`、`TcpSocket`、`SecKeyShm`
- `V2RequestCodec`、`V2RespondCodec`
- `EpollServer`、`ThreadPool`
- `BaseShm`、`RsaCrypto`、`Base64Util`、`Hash`、`Config`、`Logger`、`AesCrypto`

主要改动：

- 统一补充中文注释，重点解释模块职责、数据边界、加密边界、权限归属和资源生命周期。
- 删除正常成功路径、循环过程和调试阶段的冗余日志。
- 明确 Base64 不是加密；RSA 用于密钥交换和签名；消息正文使用 AES-GCM。
- 标注 `AesCrypto` 是历史 AES-CBC 兼容实现，新功能不应继续使用。
- 共享内存空槽检测改为 `NodeSHMInfo tmpNodeInfo{}`，避免未初始化内存参与比较。

业务影响：

- 除共享内存空槽初始化这一处可靠性修复外，其余以注释和日志清理为主。

## 4. SQL 安全治理

涉及文件：

- `ServerSeckey/ServerSeckey/mysqlOP.h`
- `ServerSeckey/ServerSeckey/mysqlOP.cpp`

主要改动：

- 新增 `escapeSqlString`，内部使用 MySQL 的 `mysql_real_escape_string` 和字符串实际长度进行转义。
- 替换 `seckeyinfo`、`message_log`、`audit_log` 的动态写入 SQL。
- 替换密钥校验、密钥注销、单条消息查询、最近消息列表查询中的动态 SQL。
- 密文、nonce、tag、审计说明和 ID 等外部字符串不再通过 `sprintf` 直接进入 SQL。
- 删除完整 SQL、密钥状态匹配、查询无结果等冗余日志，避免日志泄露敏感内容。
- 初始化 `m_conn` 为 `nullptr`，连接失败时释放对象，析构时关闭 MySQL 连接。
- `connectDb`、`checkSecKey`、`logoutSecKey` 使用 `const std::string&` 参数，减少复制。

当前边界：

- 本次采用的是最小范围的 MySQL 转义治理，未更改数据库表结构和业务协议。
- 后续仍建议升级为 `mysql_stmt` 参数化查询，并补充注入和二进制密文的自动化测试。

## 5. 代码格式规则

新增文件：

- `.clang-format`

规则：

- C++17。
- 4 空格缩进，不使用制表符。
- Allman 大括号风格。
- 控制语句与左括号之间保留空格。
- 单行长度上限为 120。
- 不自动调整头文件包含顺序，避免影响现有构建。

说明：

- 当前环境未安装 `clang-format`，因此没有对整个旧项目进行一次性格式化。
- 后续新增和修改的代码应遵循该规则；历史文件在后续触及时逐步收敛。

## 6. 关键代码改动

### 6.1 SQL 长度感知转义

文件：`ServerSeckey/ServerSeckey/mysqlOP.cpp`

```cpp
std::string mysqlOP::escapeSqlString(const std::string& value) const
{
    if (m_conn == nullptr)
    {
        return "";
    }

    std::string escaped(value.size() * 2 + 1, '\0');
    const unsigned long escapedLength = mysql_real_escape_string(
        m_conn,
        &escaped[0],
        value.data(),
        static_cast<unsigned long>(value.size()));
    escaped.resize(escapedLength);
    return escaped;
}
```

用途：替换原先的 `sprintf`。该实现会按字符串真实长度处理数据，因此 AES-GCM 密文中存在 `\0` 时也不会被截断。

### 6.2 消息记录写入

文件：`ServerSeckey/ServerSeckey/mysqlOP.cpp`

```cpp
std::ostringstream sql;
sql << "insert into message_log("
    "msg_id,sender_id,receiver_id,sender_key_id,receiver_key_id,msg_type,"
    "sender_ciphertext,sender_nonce,sender_tag,receiver_ciphertext,receiver_nonce,receiver_tag,"
    "algorithm,send_time,status) values('"
    << escapeSqlString(msgId) << "','"
    << escapeSqlString(senderId) << "','"
    << escapeSqlString(receiverId) << "',"
    << senderKeyId << ',' << receiverKeyId << ",'"
    << escapeSqlString(msgType) << "','"
    << escapeSqlString(senderCiphertext) << "','"
    << escapeSqlString(senderNonce) << "','"
    << escapeSqlString(senderTag) << "','"
    << escapeSqlString(receiverCiphertext) << "','"
    << escapeSqlString(receiverNonce) << "','"
    << escapeSqlString(receiverTag) << "','"
    << escapeSqlString(algorithm) << "','"
    << escapeSqlString(sendTime) << "'," << status << ')';
```

用途：发送方密文、接收方密文、nonce、tag 和消息元数据均先转义再写入 `message_log`。

### 6.3 密钥写入空指针保护

文件：`ServerSeckey/ServerSeckey/mysqlOP.cpp`

```cpp
if (pNode == nullptr)
{
    Logger::error("写入密钥失败: 密钥节点为空");
    return false;
}
```

用途：避免数据库写入流程在密钥节点未初始化时直接访问空指针。

### 6.4 MySQL 连接生命周期

文件：`ServerSeckey/ServerSeckey/mysqlOP.cpp`

```cpp
mysqlOP::mysqlOP()
    : m_conn(nullptr)
{
}

mysqlOP::~mysqlOP()
{
    if (m_conn != nullptr)
    {
        mysql_close(m_conn);
        m_conn = nullptr;
    }
}
```

用途：保证连接指针有确定初值，并在对象销毁时释放 MySQL 连接。

### 6.5 共享内存空槽初始化

文件：`ClientSecKey/ClientSecKey/SecKeyShm.cpp`、`ServerSeckey/ServerSeckey/SecKeyShm.cpp`

```cpp
NodeSHMInfo tmpNodeInfo{};
```

用途：替换未初始化的局部变量，避免后续 `memcmp` 比较到随机内存内容而误判共享内存空槽。

### 6.6 微秒级请求与消息 ID

文件：`ClientSecKey/ClientSecKey/ClientOP.cpp`、`ClientSecKey/ClientSecKey/MessageClient.cpp`、`ServerSeckey/ServerSeckey/MessageService.cpp`

```cpp
std::string generateClientRequestId(const std::string& clientId)
{
    static std::atomic<unsigned long long> sequence{ 0 };
    const auto now = std::chrono::system_clock::now().time_since_epoch();
    const auto micros = std::chrono::duration_cast<std::chrono::microseconds>(now).count();

    return clientId + "_" + std::to_string(micros) + "_" + std::to_string(++sequence);
}
```

服务端消息和审计 ID 使用相同结构，只是前缀改为 `serverId + "_msg"` 或 `serverId + "_audit"`。这替换了原先只使用秒级时间戳的实现。

### 6.7 单条消息查询权限

文件：`ServerSeckey/ServerSeckey/MessageService.cpp`

```cpp
if (header.sender_id() != queryResult.senderId &&
    header.sender_id() != queryResult.receiverId)
{
    respInfo.code = secmng::v2::RESULT_INVALID_REQUEST;
    respInfo.message = "permission denied";
    respInfo.serverMessageId = req.server_message_id();
    return respInfo;
}
```

用途：只有消息发送方和接收方可以查询该消息。列表查询也新增了 `req.sender_id() == header.sender_id()` 的限制，只能查询自己的发送记录。

## 7. 验证情况

已完成：

- 检查主要业务与基础设施文件，确认不再打印协商会话密钥、服务端消息明文和完整 SQL。
- 检查 `mysqlOP.cpp`，确认不存在动态 SQL 的 `sprintf` 或 `snprintf`。
- 检查 SQL 动态字段均经过 `escapeSqlString`。
- 执行补丁空白检查，未发现空白错误；仓库存在原有 LF/CRLF 行尾提示。

未完成：

- 当前 Windows 环境缺少 MySQL 开发头文件 `mysql/mysql.h`，无法完成服务端真实编译。
- 当前环境未安装 `clang-format`，无法自动执行格式化校验。

## 8. 未保留的尝试

今天曾误开始创建顶层 CMake 和公共模块构建文件，但在确认第三步实际应为 SQL 安全治理后，已完整撤回。当前项目中没有保留该部分文件或文档说明。

## 9. 下一步建议

下一步建议进行“敏感配置与演示密钥治理”：补充 `.gitignore`，区分真实配置与示例配置，检查项目中的 PEM 密钥和运行时文件是否适合公开提交。

## 10. 2026-08-08 编译验证补充

已确认项目实际编译标准为 C++17：

- `ClientSecKey/ClientSecKey/ClientSecKey.vcxproj` 已设置 `gnu++17`。
- `ServerSeckey/ServerSeckey/ServerSeckey.vcxproj` 已设置 `gnu++17`。
- 客户端和服务端均已在 WSL Ubuntu 中使用 C++17 完整编译并链接成功。
- 两个临时可执行文件的动态库检查未发现缺失依赖。
- 客户端使用 `clientA.json` 启动后能够进入命令行菜单；验证仅输入退出选项，未发起网络请求。

本次验证使用的临时构建命令需同时编译 `MessageV2.pb.cc`。它是 Protobuf 生成实现文件，不能只依赖 `*.cpp` 通配符。

仍需后续治理的告警包括：OpenSSL 3 中已弃用的传统 AES-CBC、MD5、SHA1 和 RSA 接口，以及 Protobuf 3.8 在 C++17 下触发的 `std::iterator` 弃用告警。

## 11. 2026-08-09 敏感配置与演示密钥治理

本次新增 `.gitignore` 规则，忽略本机 `server.json`、客户端 JSON 配置和 `*.pem` 密钥文件；公开仓库保留 `*.example.json` 配置模板。

新增 `docs/敏感配置与密钥说明.md`，说明可提交文件、不可提交文件、本地配置步骤和私钥或数据库密码泄露后的处理方式。

同时更新 README 和演示指南，明确真实配置、客户端私钥和服务端缓存公钥均不能提交。

随后使用 `git rm --cached` 将 4 个真实 JSON 配置和 6 个 PEM 密钥文件从 Git 索引中取消跟踪；该操作不会删除本机工作区中的文件。已逐项确认这些文件仍然存在，且新的 `.gitignore` 规则能够命中它们。后续提交后，仓库最新版本将不再包含这些敏感文件。

需要注意：取消跟踪不会清除 Git 历史中已经存在的敏感内容。如果仓库曾经推送、共享或公开，应立即轮换真实数据库密码并重新生成 RSA 密钥对；历史清理需作为独立步骤谨慎处理。本步没有修改密钥协商、网络、数据库或消息业务逻辑。

## 12. 2026-08-09 本机密钥轮换与数据库凭据校验

已为客户端 `0001` 生成新的 2048 位 RSA 私钥和公钥，并将公钥同步覆盖到服务端 `0001_pub.pem`。检查结果显示私钥有效，客户端公钥和服务端缓存公钥指纹一致。

尝试使用 `server.json` 中的现有数据库凭据连接正在运行的本机 `MySQL80` 服务。`127.0.0.1` 和 `localhost` 均返回身份验证拒绝，因此没有执行 `ALTER USER`，也没有修改 `server.json` 中的密码。这样可以避免数据库账户密码和服务端配置出现不一致。

客户端当前在每次密钥协商时都会重新生成 RSA 密钥对，所以本次替换的文件会在下一次协商时被程序更新。这是当前演示实现的既有行为，不是本次变更引入的问题。

## 13. 统一 CMake 构建入口

新增顶层 CMake 文件和客户端、服务端子目录构建文件，统一声明 C++17，并通过 CMake 查找 OpenSSL、Protobuf、JsonCpp、线程库和 MySQL 客户端库。服务端保留 `MYSQL_INCLUDE_DIR`、`MYSQL_LIBRARY` 两个可配置变量，避免把某台机器的绝对路径写入仓库。

本步不修改业务代码，不连接数据库。

Windows 本机验证未完成：默认 Visual Studio 生成器因当前环境无权访问 Windows SDK 目录而在配置前失败；Ninja 生成器因未安装 Ninja 而无法使用。两者均为本机工具链或权限限制，尚未进入项目依赖查找和源码编译阶段。后续应在可用的 WSL/Linux 工具链中执行 CMake 配置和完整编译验证。
